"""
Admin configuration for Quiz models (placeholder).
This file is prepared for future quiz functionality.
"""

from django.contrib import admin

# Register your quiz models here when they are created
# Example:
# from .models import Quiz, Question, Answer
#
# @admin.register(Quiz)
# class QuizAdmin(admin.ModelAdmin):
#     """Admin interface for Quiz model."""
#     list_display = ('title', 'course', 'time_limit', 'passing_score')
#     list_filter = ('course',)
#     search_fields = ('title',)